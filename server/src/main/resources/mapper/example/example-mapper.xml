<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.olivin.app.example.mapper.ExampleMapper">
    <!-- Interface의 메서드를 통해 실행할 쿼리 정의 -->
    <select id="selectAllList" resultType="ExampleVO">
        SELECT product_id
             , product_name
             , category_main
             , category_sub
             , vendor_name
             , product_spec
             , unit
             , pack_qty
             , safety_stock
             , purchase_price
             , sell_price
             , reg_user
             , reg_date
             , update_user
             , update_date
             , status
             , product_image
             , note
        FROM   products
    </select>
</mapper>